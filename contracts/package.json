{
  "name": "geovote-fhe-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rimraf artifacts cache deployments",
    "build": "hardhat compile",
    "node": "hardhat node",
    "deploy:localhost": "hardhat run --network localhost deploy/deploy_geovote.ts",
    "deploy:sepolia": "hardhat run --network sepolia deploy/deploy_geovote.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.12",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3",
    "@zama-fhe/relayer-sdk": "^0.3.0-5"
  },
  "dependencies": {
    "@fhevm/hardhat-plugin": "^0.3.0-1",
    "@fhevm/solidity": "^0.9.1",
    "@openzeppelin/contracts": "^5.0.2"
  }
}


